import React, {Fragment} from 'react';
import './styles/Footer.scss';

const Contact = (props) => {
  
  return(
    <Fragment>
      {/* -- Contact information -- */}
      <div>contacto</div>
    </Fragment>
  )
}

export default Contact;